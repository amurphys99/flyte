{
  "entities": {
    "Lead": {
      "title": "Lead",
      "description": "A potential customer who has signed up for early access.",
      "type": "object",
      "properties": {
        "email": {
          "type": "string",
          "format": "email",
          "description": "The email address of the lead."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp when the lead signed up."
        }
      },
      "required": ["email", "createdAt"]
    }
  },
  "firestore": {
    "/leads/{leadId}": {
      "schema": "Lead",
      "description": "Collection of early access leads."
    }
  }
}
